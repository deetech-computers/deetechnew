// components/Header.js (if you still need this)
import React from 'react';

const Header = () => {
  return (
    <header className="header">
      {/* Just header content, no dark mode toggle needed here */}
    </header>
  );
};

export default Header;